spring:
  datasource:
    url: jdbc:mariadb://pms-mariadb:3306/pms-auth?useSSL=false&serverTimezone=UTC
    username: root
    password: root

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka

management:
  tracing:
    export:
      zipkin:
        endpoint: http://zipkin:9411/api/v2/spans
