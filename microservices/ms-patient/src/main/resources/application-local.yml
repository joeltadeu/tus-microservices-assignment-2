spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/pms-patient?useSSL=false&useTimezone=true&serverTimezone=UTC
    username: root
    password: root

eureka:
  client:
    enabled: false

client:
  auth:
    url: http://localhost:8084